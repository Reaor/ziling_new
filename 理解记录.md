# 字灵核心理解记录 v2

> **文档定位**：当前对需求的精确理解。是实施前的唯一判断依据。
> **来源**：字灵最新收束.md（产品真源）+ 用户 Q&A 确认
> **最后更新**：2026-05-28（基于用户纠正）

---

## 一、字灵是什么

- 所有可见物 = 同样大小的汉字（里字）拼成的马赛克
- 一个里字 = 一个格子 = 固定字号（16px cell / 15px 字体）
- 巨字 ≠ 大字 —— 巨字是几十个里字排列成的轮廓形状，不是一个大号渲染字
- 原态字大小 = 动态字大小 —— 不存在"大号文本"
- 所有的形状（颜文字、巨字、曲线、爱心、花）本质都一样：里字走到目标格点位置排列成形

---

## 二、三模态

### 互动态（默认）

**流转**：
```
首次进入 → 假日程数据 → 里字排列成日程文本行（原态，轻微浮动）
  → 用户点/双点/拖 → 进入动态（颜文字回应）
  → 自动归位原态（AI鼓励话语）
  → 再次交互 → 动态形状（花/风车/爱心/时钟/曲线/颜文字）
```

**操作**：
- 点击 → 点击处里字散开
- 双击 → 切换当前形状（随机）
- 拖拽 → 壳心跟手，里字沿格子追（非整体平移）
- 长按 → 所有字回归原态（文本行）
- 急点 → 3次 ¬_¬ / 5次 >_< / 7次 ≥﹏≤
- 15-30秒无操作 → 自动切换形状

**颜色**：固定不可改

---

### 对话态

**进入**：左下角按钮展开输入框 → 当前形状自动变随机颜文字

**流转**：
```
[等待输入] 颜文字形状内活跃运动
  ↓ 用户发送
[AI等待期] 消息发给 DeepSeek → 颜文字运动加速、更活跃
  ↓ AI返回
═══════════════════════════

PHASE1 — 简洁回复
  → 里字内容换成 AI 回复的字
  → 从颜文字位置走向文本行布局
  → 停住，呈现给用户看
  → 用户操作（点/拖/双点）→ 进入 PHASE2
  → 长按 → 回归对话态原态
  → 新输入 → 新一轮对话

PHASE2 — 巨字形态
  → AI 指定：巨字是什么、横排竖排、轮番方案（多字轮替）、持续时长
  → 里字从文本行位置 → 沿格子走到巨字轮廓目标位置
  → 持续时长结束 → 自动进 PHASE3
  → 双击 → 直接进 PHASE3（不等时间到）
  → 拖/点/长按 → 同互动态
  → 新输入 → 新一轮对话

PHASE3 — 回复流（循环）
  → AI 返回的 stream：[{text, emoji}, {text, emoji}, ...]
  → 循环播放：原态(text) → 停顿 → 动态颜文字(emoji) → 停顿 → 下一段
  → 双击 → 打断循环 → AI从打断处重新生成新回复流
  → 拖/点/长按 → 同互动态
  → 新输入 → 新一轮对话
```

**退出**：收起输入框 → 5秒缓冲 → 切回互动态
- 缓冲期间：点/拖 → 互动态反馈，双击 → 立即切回，长按 → 对话态原态

**对话态原态**：文本行可以有轻微微动（用户确认效果不错）

**颜色**：可改变（对话态专属）

---

### 游戏态

后续完善（消消乐 + 计分 + AI 组词判定）

---

## 三、形状系统（统一逻辑）

所有形状（颜文字 ^_^、巨字 春、曲线爱心）的处理相同：

```
1. 离屏 Canvas 渲染目标形状
2. getImageData 提取非透明像素
3. 映射为格子坐标列表（掩码）
4. 通过 PIBT 引擎让里字沿格子走到掩码位置
5. 到位后在掩码允许的格子内继续闲逛
```

**约束等级**：
- 颜文字 / 巨字 → 强约束（轮廓窄，辨认度高）
- 数学曲线 / 花 / 爱心 → 弱约束（允许区域宽，更自由）

---

## 四、里字内容过渡

**原则**：同一批里字贯穿始终，不销毁重建。

**内容变化 + 位置重排**：
- AI 返回新文本 → 里字的 glyph 先变成新字 → 再走到新位置的文本行排列
- 从形状 A → 形状 B：内容可能不变，只是排列方式变

**数量自适应**：
- 目标格子数 > 里字数 → 新字从画布边缘浮现加入
- 目标格子数 < 里字数 → 多余字淡出离开画布
- "增加和减少方式是柔和的"——用户要求

**入场/退场**：
- 正常流转中：上一个形状的字 → 内容更新 → 走到新位置，不需要入场
- 需要增减时才淡入/淡出
- 首次进入页面时才从边缘入场

---

## 五、运动系统

**当前基线**（用户确认可用）：
- PIBT 引擎：60字 / 24×43格子 / 16px cell / 200ms tick / 60fps / 零碰撞
- 方向持续性 + 禁 180° 反弹
- easeInOutQuad 缓动
- 每个字独立方向变换周期（8-35 tick）

**运动要求**（来自收束文档 L23, L29）：
- 美观、匀速、沿格子、不重叠碰撞
- 偏好长段纵横（不要一格横一格竖像斜走）
- 颜文字/巨字强约束时更规整，曲线/花时更自由

---

## 六、颜文字清单（13个）

| 颜文字 | 含义 | 微动 |
|---|---|---|
| ^_^ | 开心/微笑 | 眨眼+呼吸 |
| -_- | 无语/面无表情 | 呼吸 |
| T_T | 大哭/流泪 | 眨眼+呼吸 |
| Q_Q | 哭泣 | 眨眼+呼吸 |
| U_U | 委屈/闭眼哭 | 呼吸 |
| >_< | 痛苦/抓狂/生气 | 眨眼 |
| ≥﹏≤ | 强忍泪水 | 眨眼+呼吸 |
| ¬_¬ | 怀疑/斜眼 | 眨眼 |
| =_= | 困/呆滞/咸鱼 | 呼吸 |
| ^o^ | 欢呼/兴奋 | 眨眼+嘴动 |
| ^.^ | 抿嘴笑/害羞 | 眨眼+呼吸 |
| ≥▽≤ | 大笑/超开心 | 眨眼+嘴动 |
| (^_^)/ | 挥手/打招呼 | 眨眼+呼吸 |

---

## 七、技术约束

| 约束 | 说明 |
|---|---|
| 不动运动引擎 | PIBT 核心是已验证的底层服务 |
| 零依赖 | Vanilla JS，无框架 |
| 390×700 固定 | 移动端视口硬编码 |
| 16.6ms 帧预算 | 零容忍卡顿 |
| AI 真接口 | 用 DEEPSEEK_API_KEY 环境变量，server.js 做代理转发 |
| 假日程数据 | 当前阶段用假数据 |

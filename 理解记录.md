# 字灵核心理解记录 v2

> **文档定位**：当前对需求的精确理解。是实施前的唯一判断依据。
> **来源**：字灵最新收束.md（产品真源）+ 用户 Q&A 确认
> **最后更新**：2026-05-28（基于用户纠正）

---

## 一、字灵是什么

- 所有可见物 = 同样大小的汉字（里字）拼成的马赛克
- 一个里字 = 一个格子 = 固定字号（16px cell / 15px 字体）
- 巨字 ≠ 大字 —— 巨字是几十个里字排列成的轮廓形状，不是一个大号渲染字
- 原态字大小 = 动态字大小 —— 不存在"大号文本"
- 所有的形状（颜文字、巨字、曲线、爱心、花）本质都一样：里字走到目标格点位置排列成形

---

## 二、三模态

### 互动态（默认）

**流转**：
```
首次进入 → 假日程数据 → 里字排列成日程文本行（原态，轻微浮动）
  → 用户点/双点/拖 → 进入动态（颜文字回应）
  → 自动归位原态（AI鼓励话语）
  → 再次交互 → 动态形状（花/风车/爱心/时钟/曲线/颜文字）
```

**操作**：
- 点击 → **即时响应**（不等双击判定），点击处里字做**局部、小幅、美观**的散开（不远飞、不凌乱）；并有一定概率**打破轮廓分布**（如颜文字的眼/嘴里字暂时不按轮廓排布，呈"戳乱"的俏皮反应），随后归位
- 双击 → 切换当前形状（随机）
- 拖拽 → **里字聚成紧密方环、像贪吃蛇一样环绕手指做环绕流动**，跟随手指移动并保持环绕；松手后在落点处还原为之前的形状
- 长按 → 所有字回归原态（文本行）
- 急点 → 3次 ¬_¬ / 5次 >_< / 7次 ≥﹏≤
- 15-30秒无操作 → 自动切换形状

**颜色**：固定不可改

---

### 对话态

**进入**：左下角按钮展开输入框 → 当前形状自动变随机颜文字

**流转**：
```
[等待输入] 颜文字形状内活跃运动
  ↓ 用户发送
[AI等待期] 消息发给 DeepSeek → 颜文字运动加速、更活跃
  ↓ AI返回
═══════════════════════════

PHASE1 — 简洁回复
  → 里字内容换成 AI 回复的字
  → 从颜文字位置走向文本行布局
  → 停住，呈现给用户看
  → 用户操作（点/拖/双点）→ 进入 PHASE2
  → 长按 → 回归对话态原态
  → 新输入 → 新一轮对话

PHASE2 — 巨字形态
  → AI 指定：巨字是什么、横排竖排、轮番方案（多字轮替）、持续时长
  → 里字从文本行位置 → 沿格子走到巨字轮廓目标位置
  → 持续时长结束 → 自动进 PHASE3
  → 双击 → 直接进 PHASE3（不等时间到）
  → 拖/点/长按 → 同互动态
  → 新输入 → 新一轮对话

PHASE3 — 回复流（循环）
  → AI 返回的 stream：[{text, emoji}, {text, emoji}, ...]
  → 循环播放：原态(text) → 停顿 → 动态颜文字(emoji) → 停顿 → 下一段
  → 双击 → 打断循环 → AI从打断处重新生成新回复流
  → 拖/点/长按 → 同互动态
  → 新输入 → 新一轮对话
```

**退出**：收起输入框 → 5秒缓冲 → 切回互动态
- 缓冲期间：点/拖 → 互动态反馈，双击 → 立即切回，长按 → 对话态原态

**对话态原态**：文本行可以有轻微微动（用户确认效果不错）

**颜色**：可改变（对话态专属）

---

### 游戏态

后续完善（消消乐 + 计分 + AI 组词判定）

---

## 三、形状系统（统一逻辑）

所有形状（颜文字 ^_^、巨字 春、曲线爱心）的处理相同：

```
1. 离屏 Canvas 渲染目标形状
2. getImageData 提取非透明像素
3. 映射为格子坐标列表（掩码）
4. 通过 PIBT 引擎让里字沿格子走到掩码位置
5. 到位后在掩码允许的格子内继续闲逛
```

**约束等级**：
- 颜文字 / 巨字 → 强约束（轮廓窄，辨认度高）
- 数学曲线 / 花 / 爱心 → 弱约束（允许区域宽，更自由）

---

## 四、里字内容过渡

**原则**：同一批里字贯穿始终，不销毁重建。

**内容变化 + 位置重排**：
- AI 返回新文本 → 里字的 glyph 先变成新字 → 再走到新位置的文本行排列
- 从形状 A → 形状 B：内容可能不变，只是排列方式变

**数量自适应**：
- 目标格子数 > 里字数 → 新字从画布边缘浮现加入
- 目标格子数 < 里字数 → 多余字淡出离开画布
- "增加和减少方式是柔和的"——用户要求

**入场/退场**：
- 正常流转中：上一个形状的字 → 内容更新 → 走到新位置，不需要入场
- 需要增减时才淡入/淡出
- 首次进入页面时才从边缘入场

---

## 五、运动系统

**当前基线**（用户确认可用）：
- PIBT 引擎：60字 / 24×43格子 / 16px cell / 200ms tick / 60fps / 零碰撞
- 方向持续性 + 禁 180° 反弹
- easeInOutQuad 缓动
- 每个字独立方向变换周期（8-35 tick）

**运动要求**（来自收束文档 L23, L29）：
- 美观、匀速、沿格子、不重叠碰撞
- 偏好长段纵横（不要一格横一格竖像斜走）
- 颜文字/巨字强约束时更规整，曲线/花时更自由

**关键设计决策（2026-05-29 修复"成形即冻结/点击失效/拖动迟钝"）**：
- **华容道留空**：形状掩码格子数 **故意大于** 里字数（strict 填充率约 0.82、
  loose 约 0.72）。满铺双射（格=字）会让里字成形后无空格可走而冻结，连带
  scatter（点击散开）也因四邻被冻结的里字堵死而失效。留出空格后，里字得以
  持续做华容道式滑动，点击也能正常散开。
- **strict 不再锁 anchor**：strict（颜文字/巨字）改为在掩码内做"就近"游走
  （滑向附近空格），既保持辨形又人人都在动；loose 仍在整片掩码自由远游。
- **拖动提速 tick**：拖动期引擎切到更快的固定 tick（约 85ms，常速 200ms），
  里字快速跟手追到被拖位置；松手后随动量衰减平滑回落常速。此为"模态"切速，
  不随拖拽瞬时速度变化，仍满足匀速铁律。

**关键设计决策（2026-05-29 第二轮：拖动各自路径 / 字数自适应 / 均匀辨形）**：
- **拖动各自路径（翻涌）**：去掉拖动时"全体刚性槽位重排"（那会让里字像整体
  平移、彼此相对静止）。掩码整体跟手平移后，只把目标被甩出新掩码的里字各自
  （带拖动方向偏置）重挑掩码内目标，于是它们沿各自路径翻涌着追向被拖位置，
  彼此产生相对运动，符合收束 L4"不是简单平移、像平常运动规律走到被拖位置"。
- **里字数自适应（收束 L4）**：掩码即字形本身（不再 pad 到固定数）；里字数 =
  round(掩码格数 × 填充率)，随形状大小增减。增生时新字从画布边缘淡入，减少时
  挑离形状中心最远的字淡出离场（淡入淡出时长 FADE_MS）。引擎层修了 `_nextPos`
  未随里字数变化重建的隐患，保证动态增删后 PIBT 无重叠、网格不脱同步。
- **均匀采样（辨形）**：形状采样的抽稀由"一维等距"改为 FPS 最远点采样，采样点
  均匀覆盖字形并保留"嘴"这类孤立细笔画，修复颜文字"眼多嘴少难辨形"。
- **形态目录**：新增 `>_<` 颜文字，双击循环可对比不同形态的辨形与字数增减。

**关键设计决策（2026-05-29 第三轮：环绕拖动 / 即时点击 / 细轮廓流动）**：
- **flow 流动模式（统一机制）**：引擎新增第三种约束 `flow` —— 给定一条**有序的
  格子环路**，里字按路径次序逐格向前流动（留少量空位 → 像贪吃蛇/华容道一样
  单列推进），并带少量随机扰动。该机制同时服务两个场景：
  - **拖动环绕**：拖动时里字聚成绕手指的紧密方环，沿方环流动旋转；手指移动则
    方环跟随；松手后在落点还原之前的形状（收束 L30）。
  - **细轮廓流动**：心形/曲线等细轮廓在自适应字数后，里字沿轮廓一个接一个流动
    （收束 L34），取代细轮廓上的大幅随机游走 —— 后者会破坏辨识度并使里字卡死。
- **即时点击**：手势识别不再等双击窗口确认才触发单击；抬手即触发 tap（双击则
  额外触发切换），消除点击延迟感（收束 L31）。
- **局部美观散开**：scatter 改为小幅（约 2–3 格）径向外推，不再远飞，更整洁。
- **概率打破轮廓**：点击有概率短暂解除轮廓约束让里字散成自由云团（不按轮廓
  分布），随后归位（收束 L32）。
- **强字数自适应**：复杂形状（颜文字/笔画多的汉字）配置更大的目标掩码格数，
  里字数随之增多以拼清晰（收束 L33）。

**关键设计决策（2026-05-29 第四轮：澄清"动态呈现"+ 拖动方块旋转细化）**：
- **铁律：所有动态形状里字必须运动，绝不静止**。颜文字"分布固定下来"= 固定
  *大致轮廓 + 字数*（保证辨形、眼嘴协调、数量合适），**不是**让里字定住。
  → strict（颜文字/巨字）= 轮廓与字数固定，但里字在掩码内持续华容道滑动（留
  约 20% 空位）。掩码始终保证至少 4 个空位，避免满铺把 PIBT 卡死。
- **亚像素微动（补充，非替代）**：在真实格子滑动之上叠加微小"呼吸/轻摆"位移，
  让细笔画里偶尔挪不动的字也有生命感（呼应收束 L26 眼嘴微调），不替代格子运动。
- **拖动 = 方块同心环旋转（细化收束 L30）**：以点按处为中心，里字聚成方形，按
  *同心方环逐层*绕中心旋转（非单环单列），旋转*逐渐加速*；手指移动时整个方块
  *整体平移*且持续旋转；松手在落点还原形状。修正了旧实现"局部几个字互相打转"。
- **流程约定**：复杂视觉效果由人工部署验收；自动化测试只覆盖引擎正确性（无碰撞/
  网格同步/字数一致/确有运动）。遇到需求矛盾先与产品方确认再实现。

---

## 六、颜文字清单（13个）

| 颜文字 | 含义 | 微动 |
|---|---|---|
| ^_^ | 开心/微笑 | 眨眼+呼吸 |
| -_- | 无语/面无表情 | 呼吸 |
| T_T | 大哭/流泪 | 眨眼+呼吸 |
| Q_Q | 哭泣 | 眨眼+呼吸 |
| U_U | 委屈/闭眼哭 | 呼吸 |
| >_< | 痛苦/抓狂/生气 | 眨眼 |
| ≥﹏≤ | 强忍泪水 | 眨眼+呼吸 |
| ¬_¬ | 怀疑/斜眼 | 眨眼 |
| =_= | 困/呆滞/咸鱼 | 呼吸 |
| ^o^ | 欢呼/兴奋 | 眨眼+嘴动 |
| ^.^ | 抿嘴笑/害羞 | 眨眼+呼吸 |
| ≥▽≤ | 大笑/超开心 | 眨眼+嘴动 |
| (^_^)/ | 挥手/打招呼 | 眨眼+呼吸 |

---

## 七、技术约束

| 约束 | 说明 |
|---|---|
| 不动运动引擎 | PIBT 核心是已验证的底层服务 |
| 零依赖 | Vanilla JS，无框架 |
| 390×700 固定 | 移动端视口硬编码 |
| 16.6ms 帧预算 | 零容忍卡顿 |
| AI 真接口 | 用 DEEPSEEK_API_KEY 环境变量，server.js 做代理转发 |
| 假日程数据 | 当前阶段用假数据 |

# 未纳入版本库说明

> **重要**：与前端、App、AI 的关系、接口契约、开发流程已写入仓库，供网页 AI 直接阅读：  
> → [**项目架构与集成指南.md**](./项目架构与集成指南.md)  
> → [**README.md**](./README.md)  
> → `server.example.js`、`js/ai/bridge.example.js`

本文仅说明**仍然不在 Git 历史里**的文件及原因。

---

## 一、本地副本（仓库提供 example 模板）

### `server.js`

| 项 | 说明 |
|---|---|
| **仓库中的等价物** | [`server.example.js`](./server.example.js) — 完整源码，可提交 |
| **为何还有 server.js** | 开发者复制 example 后在本地使用；`.gitignore` 防止误提交密钥或个人改动 |
| **如何获得** | `copy server.example.js server.js`，配置 `DEEPSEEK_API_KEY` 后 `node server.js` |
| **与 App 关系** | 仅开发期本地 HTTP + AI 代理；**生产由 App 后端提供 API**，见架构指南 §五、§八 |

### `js/ai/bridge.js`

| 项 | 说明 |
|---|---|
| **仓库中的等价物** | [`js/ai/bridge.example.js`](./js/ai/bridge.example.js) |
| **状态** | 待从 example 复制并实现；example 已含全部接口封装骨架 |

---

## 二、尚未开发、故无文件可提交

| 路径 | 职责 | 规格位置 |
|---|---|---|
| `js/modes/interactive.js` | 互动态 | 理解记录 §二、`技术规格.md` Phase 13 |
| `js/modes/conversation.js` | 对话态 | 理解记录 §二、架构指南 §6.3 |
| `js/modes/game.js` | 游戏态 | 理解记录 §二、架构指南 §6.4 |
| `js/ui/overlay.js` | DOM 按钮/输入框 | `技术规格.md` §2 |
| `assets/fonts/LXGWWenKai-subset.woff2` | 霞鹜文楷子集 | [LXGW WenKai](https://github.com/lxgw/LxgwWenKai)；当前用系统字体 fallback |

---

## 三、运行时/工具产物（.gitignore）

| 模式 | 说明 |
|---|---|
| `node_modules/` | 零 npm 运行时依赖；惯例忽略 |
| `cache/` | 运行时缓存（字体/AI 响应等），按需生成 |
| `*.log` | 日志 |
| `.DS_Store` | macOS 元数据 |
| `.playwright-mcp/` | 浏览器自动化调试缓存 |

---

## 四、对照清单

| 内容 | 在 GitHub？ | 说明 |
|---|---|---|
| 前端/App/AI 架构与接口 | ✅ | `项目架构与集成指南.md` |
| 本地开发服源码 | ✅ | `server.example.js` |
| AI 通信层模板 | ✅ | `js/ai/bridge.example.js` |
| 产品需求与技术规格 | ✅ | `理解记录.md`、`技术规格.md` 等 |
| 本地 `server.js` 副本 | ❌ | 从 example 复制，含环境密钥 |
| 字体二进制 | ❌ | 需自行下载 subset |
| 未实现的 modes/ui | ❌ | 待开发 |

---

## 五、`.gitignore` 摘要

```
node_modules/
.DS_Store
*.log
.playwright-mcp/
cache/
server.js
```

`server.example.js` **不在**忽略列表中，会正常提交。

---

*与 main 分支同步维护。*

# 未纳入版本库说明

本文档列出**与字灵项目相关、但未上传至 GitHub 仓库**的内容，避免克隆仓库后产生遗漏或误解。

> 仓库地址：https://github.com/Reaor/ziling_new  
> 已上传内容见仓库根目录；本文档本身已纳入版本库。

---

## 一、本地保留、刻意不上传

### 1. `server.js` — 本地开发服务器 + AI 代理

| 项 | 说明 |
|---|---|
| **作用** | 本地 HTTP 静态资源服务；代理 DeepSeek 对话接口，避免在前端暴露 API Key |
| **为何不上传** | 属于开发/调试辅助，非嵌入 App 的运行时代码；且与密钥配置强相关，写入 `.gitignore` |
| **端口** | `8080` |
| **依赖** | Node.js（内置 `http` / `fs` / `path`，无 npm 包） |

**启动方式：**

```bash
# Windows PowerShell
$env:DEEPSEEK_API_KEY = "你的 DeepSeek API Key"
node server.js

# macOS / Linux
DEEPSEEK_API_KEY=你的DeepSeek_API_Key node server.js
```

**提供的接口：**

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/api/ping` | 健康检查，返回 `{ "status": "ok" }` |
| `POST` | `/api/chat` | 请求体 `{ "message": "用户输入" }`；转发至 DeepSeek，返回 JSON（含 `quickReply`、`megachar`、`stream` 等字段） |
| `GET` | `/`、`/index.html` 及其他静态路径 | 服务项目根目录下的 HTML / JS / CSS / 字体等文件 |

**AI 回复约定（system prompt 摘要）：**

- 字灵只以 JSON 回复，不含 Markdown
- 字段：`quickReply`（20 字内）、`megachar`（关键汉字与排列方式）、`stream`（2–4 段分段回复，每段配颜文字）
- 模型：`deepseek-v4-flash`；`response_format: json_object`

**与前端关系：**

- 当前仓库内前端（`js/app.js` 等）以 Canvas 渲染与运动引擎为主，**尚未接入** `/api/chat`
- 对话态、日程 API 等见 `技术规格.md` 第十一节；`server.js` 是其中 **chat 接口** 的本地实现雏形
- 嵌入 App 时，应由宿主 App 或后端服务提供等价 API，而非依赖此文件

**获取方式：** 在完整开发工作区根目录保留 `server.js`；若仅有 Git 克隆，需向维护者索取或按上表接口自行实现等价服务。

---

### 2. `node_modules/` — npm 依赖目录

| 项 | 说明 |
|---|---|
| **现状** | 项目为 **零 npm 依赖**（Vanilla JS），通常不存在此目录 |
| **为何列入 .gitignore** | 惯例忽略；若未来引入构建/测试工具，依赖仍不应入库 |

---

### 3. `cache/` — 运行时缓存

| 项 | 说明 |
|---|---|
| **用途** | 预留本地缓存目录（如字体子集、AI 响应缓存、形状采样结果等） |
| **现状** | 当前工作区可能尚未创建；运行时按需生成 |
| **为何不上传** | 生成物，非源码 |

---

## 二、规格已规划、尚未放入仓库的实现

以下路径在 `技术规格.md` 目录树中有定义，**当前仓库尚未包含对应源文件**（非忽略，而是尚未开发完成）：

| 路径 | 职责 |
|---|---|
| `js/modes/interactive.js` | 互动态状态机 |
| `js/modes/conversation.js` | 对话态状态机 |
| `js/modes/game.js` | 游戏态（消词玩法）逻辑 |
| `js/ai/bridge.js` | 与 `/api/chat`、`/api/schedule` 等 AI 接口通信 |
| `js/ui/overlay.js` | DOM 覆盖层（按钮、输入框、提示文字） |

**当前已入库的相关代码：**

- `js/app.js` — 主入口与生命周期（部分模态逻辑仍内联或未拆分）
- `js/core/*` — 格子、里字、运动（PIBT）、形状
- `js/input/gestures.js` — 触摸手势
- `js/render/renderer.js` — Canvas 渲染
- `tests/engine-behavior.mjs` — 运动/形状等行为测试

---

## 三、规格已规划、尚未放入仓库的资源

### `assets/fonts/LXGWWenKai-subset.woff2`

| 项 | 说明 |
|---|---|
| **规格** | 技术规格选用 **霞鹜文楷（LXGW WenKai）** 子集，WOFF2，约 2MB，本地 `FontFace.load()` |
| **现状** | 仓库中 **无 `assets/` 目录**；渲染暂用系统字体 fallback：`"PingFang SC", "Microsoft YaHei", sans-serif`（见 `js/app.js`、`js/core/shape.js`） |
| **获取** | [LXGW WenKai 官方仓库](https://github.com/lxgw/LxgwWenKai) 下载字体后自行 subset；放入 `assets/fonts/` 并按 `技术规格.md` 第十四节接入 |

---

## 四、开发环境产物（与项目无关或弱相关）

以下由 `.gitignore` 排除，**不属于交付物**，克隆后无需期待存在：

| 模式 | 说明 |
|---|---|
| `.DS_Store` | macOS 文件夹元数据 |
| `*.log` | 运行日志 |
| `.playwright-mcp/` | 浏览器自动化 / MCP 调试缓存 |

---

## 五、对照清单（克隆仓库后请自行补齐）

| 类别 | 路径 / 项 | 是否在 GitHub | 你需要做什么 |
|---|---|---|---|
| 核心前端 | `index.html`, `css/`, `js/`, `tests/` | ✅ 已上传 | 直接可用 |
| 项目文档 | `技术规格.md`, `理解记录.md`, `字灵最新收束.md` | ✅ 已上传 | 阅读实施依据 |
| 本地开发服 | `server.js` | ❌ 刻意忽略 | 本地保留或按第一节自行实现 |
| 字体资源 | `assets/fonts/LXGWWenKai-subset.woff2` | ❌ 尚未添加 | 自行下载并接入 |
| 模态 / AI / UI 模块 | `js/modes/`, `js/ai/`, `js/ui/` | ❌ 尚未开发 | 按技术规格继续实现 |
| 运行时缓存 | `cache/` | ❌ 忽略 | 无需提交，运行时可建 |
| npm 依赖 | `node_modules/` | ❌ 忽略 | 当前不需要 |

---

## 六、`.gitignore` 当前规则摘要

```
node_modules/
.DS_Store
*.log
.playwright-mcp/
cache/
server.js
```

如需在团队内共享 `server.js` 模板，建议单独维护 **`server.example.js`**（不含密钥）并纳入版本库；当前策略为整文件忽略。

---

*文档版本：与仓库 main 分支同步维护。*

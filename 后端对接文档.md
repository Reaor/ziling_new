# 字灵 · 后端对接文档（API 契约）

> **给后端负责人（含其 AI 助手）：本文件是字灵前端与后端之间的唯一接口契约。**
> 按本文实现 4 个 HTTP 接口即可完成对接。前端在「无后端」时自动用内置 Mock 假数据运行，
> 因此**前端可独立演示，不会因后端缺失而报错**；你只需保证线上提供下列等价接口。
>
> 配套阅读：`项目架构与集成指南.md`（系统边界/数据流）、`字灵最新收束.md`（产品行为）、
> `理解记录.md`（精确语义）。三者有出入时，**以代码与本文为准**（详见文末「与文档的关系」）。

---

## 0. TL;DR（最少要做的事）

1. 提供 4 个接口：`GET /api/ping`、`POST /api/schedule`、`POST /api/chat`、`POST /api/validate`。
2. 它们的**请求/响应 JSON 形状**严格如下文（前端按字段名取值，多余字段忽略）。
3. AI 密钥**只放在后端环境变量**，前端永不接触。
4. 给 WebView 注入 `window.ZILING_CONFIG = { apiBase: "https://你的后端/ziling" }`（无尾斜杠）。
5. 前端会把请求发到 `apiBase + 路径`，例如 `https://你的后端/ziling/api/chat`。

> 如果你只想先让前端跑起来：**什么都不用做**。前端 `aiSource()` 检测不到 `apiBase` 时
> 自动走 Mock。等接口就绪，注入 `apiBase` 即切到真实后端，前端代码零改动。

---

## 1. 总体数据流

```
WebView(字灵前端)  ──HTTPS──▶  你的后端网关(/ziling/api/*)  ──▶  大模型(DeepSeek 等)
        ▲                                  │
        └──────────  JSON（下文契约） ◀──────┘
```

- 前端只认 JSON；不关心你后端用什么语言/模型。
- 前端调用封装在 `js/ai/bridge.js`，三处来源自动择优：
  1. `window.ZILING_CONFIG.apiBase`（**你提供的后端** → 生产）
  2. 用户本机自带 key（仅调试，浏览器直连，key 存 localStorage，不上传）
  3. 内置 Mock（无网络也能演示）

---

## 2. 接口契约（逐个）

所有接口：`Content-Type: application/json`；HTTP 200 表示成功；非 200 前端会回退 Mock。

### 2.1 `GET /api/ping` — 健康检查

**响应：**
```json
{ "status": "ok" }
```

### 2.2 `POST /api/schedule` — 互动态·日程反馈

进入宠物页时，前端把当日日程发给你，你回一句陪伴的话 + 一个颜文字。

**请求：**
```json
{
  "completed": [{ "title": "写周报", "time": "14:00" }],
  "pending":   [{ "title": "改方案", "deadline": "18:00" }],
  "delayed":   [{ "title": "整理文档", "reason": "优先级调整" }]
}
```
（三个数组都可能为空；元素里前端只依赖 `title`，其余字段你可自由使用。）

**响应：**
```json
{ "message": "今天完成了3件事，还有1件拖延了哦", "emoji": "^_^" }
```
| 字段 | 说明 | 约束 |
|---|---|---|
| `message` | 回归原态后呈现的一句话 | ≤30 字，中文，**不得负面攻击** |
| `emoji` | 先以动态颜文字呈现 | **必须**是 §4 颜文字白名单之一 |

### 2.3 `POST /api/chat` — 对话态·三阶段

用户在对话框发消息，你返回驱动「PHASE1 简洁回复 → PHASE2 巨字 → PHASE3 回复流循环」。

**请求：**
```json
{
  "message": "今天好累啊",
  "history": [
    { "role": "user", "content": "上一句用户说的" },
    { "role": "assistant", "content": "上一句字灵回复的 quickReply" }
  ]
}
```
（`history` 是最近若干轮，最多约 8 条；首次对话为空数组。你可用它做多轮记忆。）

**响应：**
```json
{
  "quickReply": "累的时候休息一下也是修行呢",
  "megachar": { "chars": ["休"], "direction": "vertical", "rotateInterval": 0, "duration": 3000 },
  "stream": [
    { "text": "你看，连休字都是人靠在木旁", "emoji": "^_^",  "word": "休息" },
    { "text": "古人早就知道休息的重要了",   "emoji": "-_-" },
    { "text": "去喝杯水、伸个懒腰吧",       "emoji": "(^_^)/", "word": "从容" }
  ]
}
```
| 字段 | 对应阶段 | 约束 |
|---|---|---|
| `quickReply` | PHASE1：变成原态文本行 | ≤20 字 |
| `megachar.chars` | PHASE2：里字拼成的巨字/词 | **1~4 个汉字（数组），可为词语/成语**；前端拼接呈现 |
| `megachar.direction` | 横/竖排提示 | `"vertical"` / `"horizontal"`（竖屏窄，前端按页面自适应一次几字/轮播） |
| `megachar.rotateInterval` | 多字分组轮播间隔(ms) | `0`/缺省 = 用前端默认；前端**字多自动分组轮播、字少整体呈现** |
| `megachar.duration` | PHASE2 停留时长(ms) | 建议 2500~4000；前端会**等形态成型后再停留**这么久 |
| `stream[].word`（可选） | PHASE3 该段巨字内容 | 词语/成语；缺省时前端会随机挑一个应景词或用颜文字（巨字概率约 0.55） |
| `stream[]` | PHASE3：循环呈现 | 每段 `text`(一句话)→原态，`emoji`→动态颜文字；建议 3~5 段 |
| `stream[].emoji` | | **必须**是 §4 白名单之一 |

> PHASE3 是**循环**的：前端会把 `stream` 一段段轮播，直到用户交互/收起对话框。

### 2.4 `POST /api/validate` — 游戏态·组词判定（后续，先留接口）

> 游戏态本期不实现，**接口先占位**即可（可暂时恒返回 `{"valid":false}`）。

**请求：** `{ "char1": "明", "char2": "天" }`
**响应：** `{ "valid": true, "word": "明天" }`（顺序不论；不成词返回 `{ "valid": false }`）

---

## 3. 推荐的后端实现骨架（Node 示例，可直接给你的 AI 参考）

仓库根目录的 `server.example.js` 已是**可运行的开发期实现**（静态托管 + `/api/chat` 代理 DeepSeek）。
生产后端做等价的事即可。最小骨架：

```js
// 伪代码：表达每个接口该做什么
app.get('/api/ping', (req, res) => res.json({ status: 'ok' }));

app.post('/api/schedule', async (req, res) => {
  const sys = '你叫"字灵"。据用户今日日程完成情况，输出 {"message","emoji"}。emoji 限白名单。';
  const out = await callLLM(sys, JSON.stringify(req.body)); // 你的大模型调用
  res.json(safeSchedule(out));   // 校验 emoji 在白名单、message 长度
});

app.post('/api/chat', async (req, res) => {
  const sys = '你叫"字灵"…输出 {quickReply, megachar, stream}…';   // 见 server.example.js 完整提示词
  const out = await callLLM(sys, req.body.message, req.body.history);
  res.json(safeChat(out));
});

app.post('/api/validate', async (req, res) => {
  res.json(await isWord(req.body.char1, req.body.char2)); // 词库或 LLM 判定
});
```

**强烈建议**：在后端做一层 `safeXxx()` 兜底——校验字段存在、`emoji` 落在白名单、长度合理、
解析失败时给一个安全默认值。前端虽有回退，但后端兜底能避免"AI 偶发乱输出导致呈现异常"。

---

## 4. 颜文字白名单（emoji 字段只能取这些）

前端只内置了这些颜文字形状，**返回白名单外的值会被忽略/降级为 `^_^`**：

```
^_^   -_-   T_T   Q_Q   U_U   >_<   ≥﹏≤   ¬_¬   =_=   ⊙_⊙   ^o^   ^.^   ≥▽≤   (^_^)/
```

含义参考（用于挑选合适情绪，详见 `字灵最新收束.md` 第 6~20 条）：
开心 `^_^` / 无语 `-_-` / 大哭 `T_T` / 哭泣 `Q_Q` / 委屈 `U_U` / 抓狂 `>_<` /
强忍泪 `≥﹏≤` / 怀疑 `¬_¬` / 困 `=_=` / 震惊 `⊙_⊙` / 欢呼 `^o^` / 害羞 `^.^` /
大笑 `≥▽≤` / 打招呼 `(^_^)/`。

---

## 5. 日程数据如何进入前端（宿主 App ↔ WebView）

三选一（前端三种都已支持，见 `js/app.js` 的 `wireScheduleIntake`）：

```js
// A. URL 参数（首次加载）
//    index.html?schedule={"completed":[...],"pending":[...],"delayed":[...]}

// B. postMessage（推荐，可动态更新）
webview.postMessage({ type: 'ziling:schedule', payload: { completed:[], pending:[], delayed:[] } });

// C. 注入全局函数（Android/iOS 原生调用）
window.setSchedule({ completed:[], pending:[], delayed:[] });
```

注入配置（让前端知道后端地址）：
```js
window.ZILING_CONFIG = {
  apiBase: 'https://your-app-backend.com/ziling', // 无尾斜杠
  schedule: { /* 可选：初始日程，等价于调用一次 setSchedule */ }
};
```

---

## 6. 「人设/语气」可配置（替代"让 AI 改文档"）

字灵的性格、语气、回复风格由**后端的系统提示词（system prompt）**决定，不由前端写死，
也**不需要**一个"AI 去改前端文档"的机制。要调性格：改你后端 `callLLM` 的 system prompt 即可。

如果希望宿主 App 能动态下发人设（如不同用户不同语气），建议你后端接受一个可选配置并并入
system prompt（前端无需改动）：
```json
// 你后端可自定义的内部配置（非前端契约的一部分，前端不感知）
{ "persona": "温柔耐心的学习伙伴", "tone": "口语化、偶尔俏皮", "maxReplyLen": 20 }
```
这样"按记忆/按需求调整字灵表现"完全在后端可控范围内完成，稳定且可回滚。

---

## 7. 联调清单（按顺序自检）

- [ ] `curl https://你的后端/ziling/api/ping` 返回 `{"status":"ok"}`
- [ ] `POST /api/schedule` 返回含 `message`、白名单内 `emoji`
- [ ] `POST /api/chat` 返回含 `quickReply`、`megachar.chars`、`stream[]`
- [ ] WebView 注入 `window.ZILING_CONFIG.apiBase`，前端调试面板「AI · 来源」显示 `backend`
- [ ] 断网/后端 500 时前端不崩（自动回退 Mock）
- [ ] 所有 `emoji` 落在 §4 白名单

---

## 8. 与三份文档的关系（避免"AI 找不到东西/互相矛盾"）

| 文档 | 角色 | 出现矛盾时 |
|---|---|---|
| **本文 `后端对接文档.md`** | 接口契约（前后端唯一事实源） | **最高优先级**（接口以本文+代码为准） |
| `项目架构与集成指南.md` | 系统边界、数据流、目录分层 | 架构层面以它为准 |
| `字灵最新收束.md` | 产品行为/交互语义（中文叙述） | 行为语义参考；与实现冲突时以**代码**为准 |
| `理解记录.md` | 行为的精确补充说明 | 同上 |

> 实现已对上述文档做过多轮迭代，**凡后续代码改动与文档不一致，一律以代码为准**，
> 并在改动 PR 里同步订正文档（本仓库已按此惯例维护）。
> 后端 AI 若发现"文档说有但代码没有"的功能（如游戏态），以本文「实现状态」标注为准，不要臆测。

---

## 9. 各接口实现状态（截至当前）

| 接口 | 前端 | 开发期 server.example.js | 生产后端 |
|---|---|---|---|
| `/api/ping` | ✅ 调用 + Mock 兜底 | ✅ | 待你实现 |
| `/api/schedule` | ✅ 调用 + Mock 兜底 | ⚠️ 待补（前端 Mock 已可演示） | 待你实现 |
| `/api/chat` | ✅ 调用 + Mock 兜底 | ✅ DeepSeek 代理 | 待你实现 |
| `/api/validate` | ✅ 调用 + Mock 兜底 | ❌ | 后续（游戏态） |

*与 main 分支同步维护。*

# 字灵 × 日程管理 App · 结合呈现与设计文档

> 面向产品/设计/合作方：说明「字灵」作为日程管理 App 内的一个情感化伴随模块，**是什么、能做什么、
> 如何与日程 App 结合、人机交互上的考量**。技术接口见 `后端对接文档.md`，工程细节见 `项目架构与集成指南.md`。

---

## 一、一句话定位

**字灵**是嵌入日程管理 App 的一个「汉字精灵」WebView 宠物页：用成百上千个统一大小的**里字**（汉字）
聚散成形状、沿格子匀速流动，把用户的**日程完成情况**与**对话**转化为有温度的视觉陪伴——
鼓励、安慰、调侃、提醒，让"管理日程"这件略带压力的事多一点情绪价值。

它不是独立产品，而是 App 里一块**会呼吸、会回应**的活的区域。

---

## 二、为什么把它放进日程 App（产品意义）

日程类 App 的核心痛点不是"记不下来"，而是**坚持难、有压力、缺正反馈**。字灵的价值在于：

| 用户处境 | 字灵的回应 |
|---|---|
| 今天完成了好几件 | 聚成笑脸颜文字 + 巨字"赞/成"，一句"今天很棒" |
| 有任务拖延了 | 温和的安慰颜文字 + "慢慢来，挑一件先开始" |
| 心情低落来倾诉 | 对话态：先一句简短回应，再用巨字（如"休/安"）+ 一段段陪伴的话 |
| 只是想放松一下 | 互动态：拖动里字成旋转字团、点击散开、看各种优美动态形状 |

**情绪价值 + 轻游戏化**，降低日程管理的"任务感"，提升打开频率与坚持度。

---

## 三、字灵的三种模态（用户视角）

### 1. 互动态（默认）
打开宠物页即进入。开屏先播一段动画（细线如电路汇聚 → `^_^` 浮现并眨眼），随后呈现当日日程。
之后里字会在「**原态**（像正常文本行的一句话）」与「**动态**（聚成形状）」间切换：

- **点击**：点处里字散开再归位（即时反馈）
- **双击**：切换下一个动态形状
- **长按**：回到原态那句话
- **拖动**：里字聚成旋转方形团、环绕手指；松手落回原形状

动态形状库很丰富：颜文字（喜怒哀乐）、数学曲线（心形/玫瑰/蝴蝶/李萨如/旋轮花…）、
动态曲线（正弦波/水波/DNA 双螺旋/旋涡/银河/钟摆/摆群波…）、即时北京时间、贪吃蛇等。

### 2. 对话态
点左下角 💬 展开输入框，对字灵说话。AI 回复分三段呈现，**每段成型后停留、再自然切换**：
1. **简洁回应** → 排成原态文本行（一句话）
2. **关键巨字/词** → 里字拼成的大字（如"休息""从容""守得云开"），自适应页面一次几字或轮播
3. **回复流** → 一段段陪伴的话与颜文字/巨字交替循环，直到用户再次交互

### 3. 游戏态
点左上角 🎮 进入「文字消消乐」：规整格子里每格一个字，点两个能组成词的字即可消除
（顺序不论），墨色溢出特效 + 计分 + 补字。判定优先本地词库（即时），兜底问 AI。

---

## 四、与日程 App 的结合点（数据 / 时机 / 呈现）

### 4.1 日程数据如何进来
App 在加载/更新时把当日日程传给字灵（三选一，均已支持）：
- URL 参数 `?schedule=...`
- `postMessage({type:'ziling:schedule', payload})`
- 注入 `window.setSchedule(data)`

日程结构（`completed / pending / delayed` 三类，每项有 `title` 等）见 `后端对接文档.md §5`。

### 4.2 进入宠物页的呈现节奏（已实现）
```
开屏动画 → 取日程 → 一段"AI 输出流"（据完成/拖延情况鼓励·激励·安慰，配巨字/颜文字）
        → 并入互动态：随机切优美形状，偶尔再冒出一句 AI 回复（保持趣味）
```
用户任意交互（点/拖/双击/长按）即接管，自动播放让位于用户操作。

### 4.3 反馈风格可调
字灵的语气/人设由**后端的系统提示词**决定（也可在设置页填"AI 风格"临时调整）。
例如让它更俏皮、更古典文艺、更简洁——无需改前端。

---

## 五、人机交互 / 人因工程考量

字灵的每个交互细节都围绕"**即时、可预期、不打扰、可控**"设计：

- **即时反馈**：点击立刻有散开反应，绝不让用户以为没响应（移动端尤其重要）。
- **手势容错**：双击用"时间窗 + 像素距离容差"判定，适配移动端手指偏移；长按需明显停顿，
  避免与"拖着玩"误触。
- **匀速、不闪烁**：所有里字同一时刻速度一致、严格沿格子走、绝不重叠/闪现——视觉稳定、不晕。
- **形态成型后再切换**：复杂形状/巨字成型需要时间，系统会等它"长好"并停留观赏后才切，
  避免"刚拼出来就没了"的挫败。
- **过渡平滑**：原态↔动态之间，多余的字螺旋淡出、新增的字螺旋淡入，绝不"啪地消失/冒出"。
- **可控与个性化**：深浅色（跟随系统）、字体、字色（含呼吸渐变/炫彩）、原态字号、
  AI 风格、AI 记忆开关均可在设置页调整，并记住选择。
- **延时友好**（游戏判词、AI 回复）：本地即时兜底 + 等待动效（脉动/活跃颜文字）缓解延迟的负反馈。
- **隐私**：AI 密钥只在后端；前端从不内置密钥。

---

## 六、运营 / 商业化可挂载点（给后端/产品取舍）

字灵的「设置页」本身被设计成**呈现给后端做取舍与再设计**的页面。可在其框架上扩展运营逻辑，例如：

- **成就解锁**：连续完成日程 N 天 → 解锁某种字色 / 呼吸炫彩 / 限定形态。
- **节日彩蛋**：特定日期呈现应景巨字（如"福""新年好"）或专属颜文字。
- **会员特权**：高级字体、更长 AI 记忆、专属人设。
- **日程联动激励**：完成率里程碑触发特别的庆祝形态与话语。

这些都可在不动运动引擎的前提下，由后端通过配置/提示词 + 现有接口实现。

---

## 七、技术形态速览（合作方了解即可）

- 纯前端零依赖（Vanilla JS + Canvas），嵌入 iOS/Android WebView。
- 运动引擎（PIBT 走格子）+ 形状采样 + 三模态协调 + AI 通信层（后端/自带 key/Mock 三态择优）。
- 无后端/无密钥也能完整演示（内置 Mock 假数据）。
- 详见 `项目架构与集成指南.md`、`后端对接文档.md`。

---

*与实现同步维护；如与代码冲突以代码为准。*

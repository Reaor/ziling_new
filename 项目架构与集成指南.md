# 项目架构与集成指南

> **本文档面向后续用网页 AI 继续开发的场景。**  
> 说明字灵前端、宿主 App、本地开发服、AI 后端之间的边界、数据流与接口契约。  
> 读完本文 + `理解记录.md` 即可开始改代码，无需猜测隐含关系。

---

## 一、字灵在整体产品中的位置

```
┌─────────────────────────────────────────────────────────┐
│                    宿主 App（日程 / 账户 / 导航）           │
│  ┌───────────────────────────────────────────────────┐  │
│  │              WebView（字灵宠物页）                    │  │
│  │  index.html + Canvas(里字渲染) + DOM UI(按钮/输入框)  │  │
│  │  js/app.js 协调三模态 + 运动引擎 + AI 内容呈现        │  │
│  └───────────────────────────────────────────────────┘  │
│         │ postMessage / 注入 API          │ HTTPS         │
│         ▼                                 ▼               │
│   日程数据 completed/pending/delayed    App 后端或开发期    │
│                                         server.js 代理     │
│                                              │             │
│                                              ▼             │
│                                        DeepSeek API        │
└─────────────────────────────────────────────────────────┘
```

**字灵不是独立产品**，而是 App 内一个 **390×700 的 WebView 页面**：

- **宿主 App 负责**：用户账号、真实日程数据、是否在 App 内提供 AI 网关（生产环境）
- **字灵前端负责**：里字的渲染、运动、手势、三模态 UI 与呈现逻辑
- **AI 服务负责**：根据日程/对话/组词请求返回结构化 JSON，驱动里字内容与形状变化

---

## 二、三种运行形态对比

| 形态 | 如何启动 | 静态资源 | AI 接口 | 日程数据 |
|---|---|---|---|---|
| **A. 浏览器 + 本地 server** | `node server.js` → `localhost:8080` | server 托管仓库文件 | `POST /api/chat`（server 代理 DeepSeek） | 前端硬编码假数据 |
| **B. App 内 WebView（目标）** | App 加载打包后的 `index.html` | `file://` 或 App 内置 HTTP | App 后端 URL 或 WebView 注入的全局函数 | App `postMessage` / `setSchedule()` |
| **C. 纯静态预览（受限）** | 直接打开 html | 本地文件 | ❌ 无（且 ES Module 可能失败） | 假数据 |

**开发期推荐形态 A**；与 App 联调时用形态 B。

---

## 三、前端内部分层（当前 vs 目标）

```
app.js                 主控制器：模态切换、rAF 循环、组装各模块
├── modes/             三模态状态机（待建）
│   ├── interactive.js   互动态：日程原态↔动态、急点、自动切形状
│   ├── conversation.js  对话态：三阶段 PHASE1/2/3、中断、收起
│   └── game.js            游戏态：8×8 消词、计分、AI 判词
├── ui/overlay.js      DOM：游戏入口、对话入口、输入框、提示（待建）
├── ai/bridge.js       统一 HTTP 调用 /api/*（待建，见 bridge.example.js）
├── core/              ✅ 已实现
│   ├── grid.js          格子占用
│   ├── character.js     里字 + 对象池
│   ├── motion.js        PIBT 运动、形状约束、拖拽、散开
│   └── shape.js         离屏采样、颜文字/巨字掩码
├── render/renderer.js   ✅ Canvas DPR、clear、getContext
└── input/gestures.js  ✅ tap / double-tap / long-press / drag
```

### 当前 `app.js` 实际做了什么

- 初始化 Renderer、Grid、CharacterPool(200)、MotionEngine(PIBT)
- 放置 60 个测试里字，3 秒后切矩形形状 demo
- 接入手势：点击散开、拖拽形状、debug 模式 `?debug=1`
- **尚未实现**：模态、UI 按钮、AI 调用、日程、对话流、游戏

### 关键设计约束（改代码时必须遵守）

1. **不动 PIBT 运动引擎核心**——已在 60 字规模验证，上层只调用公开 API
2. **里字不销毁重建**——内容变化改 glyph，数量变化淡入淡出（见 `理解记录.md` 第四节）
3. **巨字 = 里字排列成的轮廓**，不是大号 `fillText`
4. **帧预算 16.6ms**，路径规划跨帧分摊，禁止单帧规划 >5 条路径
5. **零 npm 运行时依赖**

---

## 四、三模态与 UI 入口（产品 ↔ 代码映射）

| 模态 | 用户如何进入 | 对应模块 | 颜色 |
|---|---|---|---|
| **互动态**（默认） | 打开宠物页 / 退出其他模态 | `modes/interactive.js` | 固定 `#1a1a2e` |
| **游戏态** | 左上角 36×36 按钮 | `modes/game.js` | 固定 |
| **对话态** | 左下角展开输入框并发送 | `modes/conversation.js` | AI 或用户可改 |

DOM 结构已在 `index.html` 预留：

```html
<div id="app-container">
  <canvas id="main-canvas"></canvas>
  <div id="ui-overlay"></div>  <!-- overlay.js 在此挂载按钮/输入框 -->
</div>
```

按钮规格见 `技术规格.md` §2.2；样式基座在 `css/app.css`（`ui-overlay` 子元素可点）。

---

## 五、宿主 App ↔ WebView 集成

### 5.1 App 向字灵传递日程（互动态入场）

规格定义三种方式（`技术规格.md` §25.2）：

```javascript
// 方式 A：URL 参数（适合首次加载）
// index.html?schedule={"completed":[...],"pending":[...],"delayed":[...]}

// 方式 B：postMessage（推荐，可动态更新）
window.addEventListener('message', (e) => {
  if (e.data?.type === 'ziling:schedule') {
    handleSchedule(e.data.payload);
  }
});

// 方式 C：Android WebView 注入
window.setSchedule = function (data) { handleSchedule(data); };
```

**日程数据结构：**

```json
{
  "completed": [{ "title": "完成项目报告", "time": "14:00" }],
  "pending":   [{ "title": "回复邮件", "deadline": "18:00" }],
  "delayed":   [{ "title": "整理文档", "reason": "优先级调整" }]
}
```

**前端收到日程后应：**

1. 将日程文本转为里字内容，排列成**原态文本行**（轻微浮动）
2. 调用 `POST /api/schedule` 获取 AI 反馈（emoji + message）
3. 用户首次点/拖/双击后，按 AI 反馈进入动态颜文字，再回归原态

当前阶段：**假日程硬编码**即可，接口形状不变。

### 5.2 App 向字灵提供 AI 基址（生产环境）

开发期前端与 `server.js` 同源，`fetch('/api/chat')` 即可。

上线后 App 应通过以下之一告知 API 根路径：

```javascript
// 建议：WebView 加载前注入
window.ZILING_CONFIG = {
  apiBase: 'https://your-app-backend.com/ziling',  // 无尾斜杠
  schedule: { /* 可选初始日程 */ }
};
```

`js/ai/bridge.js` 应读取 `window.ZILING_CONFIG?.apiBase ?? ''` 拼接请求。

### 5.3 WebView 配置建议

| 平台 | 要点 |
|---|---|
| iOS WKWebView | 允许 JS、`file://` 读本地 bundle；或内置 localhost |
| Android WebView | `WebViewAssetLoader` 或 `loadUrl("file:///android_asset/...")`；启用 DOM Storage |
| 通用 | 禁用用户缩放；`touch-action: none` 已在 CSS 层需配合 canvas |

---

## 六、AI 接口契约（前端 / server / App 后端统一）

以下路径在 **`server.example.js`** 中部分已实现；其余为 `技术规格.md` §11 定义，**App 后端应提供等价实现**。

### 6.1 `GET /api/ping`

健康检查。

```json
{ "status": "ok" }
```

### 6.2 `POST /api/schedule` — 互动态日程反馈

**请求：**

```json
{
  "completed": [{ "title": " string", "time": "HH:mm" }],
  "pending":   [{ "title": " string", "deadline": "HH:mm" }],
  "delayed":   [{ "title": " string", "reason": " string" }]
}
```

**响应：**

```json
{
  "message": "今天完成了3件事，还有1件拖延了哦",
  "emoji": "^_^"
}
```

前端用 `emoji` 驱动颜文字形状，用 `message` 作为回归原态后的文本行内容。

**实现状态：** `server.example.js` 未实现 → 待开发或 App 后端提供。

### 6.3 `POST /api/chat` — 对话态

**请求：**

```json
{
  "message": "今天好累啊",
  "history": [
    { "role": "user", "content": "..." },
    { "role": "assistant", "content": "..." }
  ]
}
```

**响应（驱动对话态三阶段）：**

```json
{
  "quickReply": "累的时候休息一下也是修行呢",
  "megachar": {
    "chars": ["休"],
    "direction": "vertical",
    "rotateInterval": 0,
    "duration": 3000
  },
  "stream": [
    { "text": "你看，连休字都是人靠在木旁", "emoji": "^_^" },
    { "text": "古人早就知道休息的重要了",   "emoji": ">=v<=" },
    { "text": "去喝杯水、伸个懒腰吧",       "emoji": "(^_^)/" }
  ]
}
```

| 字段 | 对话态阶段 |
|---|---|
| `quickReply` | PHASE1 简洁回复 → 文本行原态 |
| `megachar` | PHASE2 巨字形态（chars、横/竖、轮替 interval、持续 duration） |
| `stream[]` | PHASE3 回复流循环：每段 text→原态，emoji→动态颜文字 |

**实现状态：** ✅ `server.example.js` 已实现（DeepSeek `deepseek-v4-flash`，JSON mode）。

**对话态流转详见** `理解记录.md` §二、`技术规格.md` 对话态章节。

### 6.4 `POST /api/validate` — 游戏态组词判定

**请求：** `{ "char1": "明", "char2": "天" }`  
**响应：** `{ "valid": true, "word": "明天" }`

**实现状态：** 未实现。

---

## 七、前端 AI 通信层（待实现 `js/ai/bridge.js`）

模板已提供：**`js/ai/bridge.example.js`**。实现时应：

1. 从 `window.ZILING_CONFIG.apiBase` 取基址
2. 封装 `ping()` / `schedule()` / `chat()` / `validate()`
3. 对话请求期间触发 `conversation.js` 的「等待态」— 颜文字运动加速
4. 解析 JSON 后交给对应模态，**不**在 bridge 内操作 Canvas

```javascript
// 伪代码：app.js 中的用法
import { chat } from './ai/bridge.js';

async function onUserSend(text) {
  enterWaitingMotion();           // conversation.js
  const data = await chat(text, history);
  startPhase1(data.quickReply);   // 里字换内容 → 走向文本行
  // … 后续由 conversation 状态机接管 megachar / stream
}
```

---

## 八、本地开发服 `server.js` 与仓库的关系

| 文件 | 在仓库？ | 说明 |
|---|---|---|
| `server.example.js` | ✅ | 完整可运行模板，**无密钥** |
| `server.js` | ❌ `.gitignore` | 本地复制自 example，可含个人配置 |

```bash
copy server.example.js server.js   # Windows
cp server.example.js server.js     # macOS/Linux

$env:DEEPSEEK_API_KEY = "sk-..."
node server.js
# → http://localhost:8080
```

**为何 ignore `server.js` 而上传 example：** 避免误提交密钥或本地改动；AI 与协作者以 example 为准。

**server 职责边界：**

- ✅ 静态托管 `index.html` / `js` / `css` / `assets`
- ✅ 开发期 CORS + DeepSeek 代理
- ❌ 不是 App 生产后端；不上线部署

---

## 九、内容如何驱动里字（AI 响应 → 画面）

统一管线（所有形状相同逻辑，见 `理解记录.md` §三）：

```
AI JSON → 更新里字 glyph 列表 → shape.js 生成掩码 → motion.setShapeMask
       → PIBT 沿格子走到目标 → 到位后掩码内闲逛
```

**数量自适应：**

- 目标格 > 里字数 → 边缘淡入新里字
- 目标格 < 里字数 → 多余里字淡出

**模态切换时**里字对象不销毁，只改内容与目标格。

---

## 十、实施 Phase 与仓库现状对照

| Phase | 内容 | 仓库状态 |
|---|---|---|
| 0–4 | 字体/Canvas/格子/单字/多字运动 | ⚠️ 字体用系统 fallback；运动 ✅ |
| 5–9 | 形状/切换/拖拽/点击散开 | ✅ 核心在 `shape.js` + `app.js` demo |
| 10–12 | 300字/三层运动/手势 | ✅ 手势；300字压测待做 |
| 13 | 互动态状态机 | ❌ |
| 14 | 对话态状态机 | ❌ |
| 15–17 | 文本行/游戏/原态动画 | ❌ |
| 18 | 入场动画 + 日程桥接 | ❌ |
| **19** | **AI 集成** | ⚠️ server 有 chat；前端 bridge 未接 |
| 20–21 | 设置/真机 | ❌ |

**网页 AI 建议下一批任务（按依赖顺序）：**

1. 实现 `js/ai/bridge.js`（从 example 复制）
2. 实现 `js/ui/overlay.js` — 四个按钮 + 输入框
3. 实现 `js/modes/conversation.js` — 先打通 chat 三阶段
4. 实现 `js/modes/interactive.js` — 假日程 + schedule API
5. 把 `app.js` 中 demo 逻辑迁入 modals，app.js 只做编排

---

## 十一、给网页 AI 的阅读顺序

1. **本文** — 架构与接口
2. **`理解记录.md`** — 产品行为精确语义
3. **`js/app.js`** — 当前入口与 demo 行为
4. **`js/core/motion.js`** — PIBT 公开 API（`registerCharacter`, `setShapeMask`, `scatter`, `beginShapeDrag`…）
5. **`技术规格.md`** — 细节算法与 Phase 验收标准
6. **`server.example.js`** + **`js/ai/bridge.example.js`** — 联调 AI 时对照

---

## 十二、测试

```bash
node tests/engine-behavior.mjs
```

验证运动/形状相关行为（无浏览器）。

浏览器手动测试：`http://localhost:8080?debug=1` 查看 FPS 与碰撞检测 overlay。

---

*与 main 分支同步维护。*

# 交接给后端 / 队友（zicodo 对接说明）

> 写给负责 zicodo 后端（Express + Sequelize + SQLite + JWT）与服务器的同学。
> 前端已按 `qiadastrachen-bit/zicodo` master 的**真实源码契约**完成对接（逐文件核对过
> routes/controllers/models），不是按文档猜的。本文说明：怎么接、前端用了哪些接口、
> 字段怎么映射、哪些功能是前端本地实现、建议后端补什么。

在线预览（纯前端 / 本地模式）：https://reaor.github.io/ziling_new/

---

## 一、你只需要做三件事

1. **起后端**：按 zicodo 仓库的 Docker 方式部署（Nginx + Express）。
   `.env` 里 `CORS_ORIGIN` 必须放行前端域名（联调期可设 `*`；若由你的 Nginx
   同域托管本前端则无 CORS 问题）。
2. **告诉前端后端在哪**（三选一，推荐 ①）：
   - ① **同域托管（零配置）**：把本仓库的静态文件放进你的 Nginx 跟 API 同域。
     前端默认请求同源，什么都不用改。
   - ② 改一行配置：`js/shell/config.js` 里 `API_BASE = 'https://你的后端'`，随前端发布。
   - ③ 联调期临时用：打开 `index.html?api=https://你的后端`（会记在浏览器本机）。
3. **验证**：`GET /api/health` 返回 `{status:'ok'}` 后，App「我的 → 登录/注册」会自动
   从"后端未接入"变成登录表单。注册/游客登录 → 建队拿邀请码 → 第二台设备加入 → 通了。

> 用户**永远不需要**填服务器地址；探活失败时登录入口显示友好的"未接入"说明，
> App 以本地模式照常可用。

---

## 二、前端实际调用的接口（已实现）

认证头 `Authorization: Bearer <JWT>`；统一响应 `{code,message,data}`，`code===0` 成功；
`1001/1002` 时前端自动清 token 登出。

| 用途 | 端点 |
|---|---|
| 探活 | `GET /api/health` |
| 注册 / 登录 / 游客 / 当前用户 | `POST /api/auth/register`、`POST /api/auth/login`、`POST /api/auth/auto-login`、`GET /api/auth/me` |
| 任务 | `GET /api/tasks`、`POST /api/tasks`、`PUT /api/tasks/:id`、`DELETE /api/tasks/:id`、`POST /api/tasks/:id/check`（完成=打卡；3003 已打卡视为幂等成功） |
| 团队（多团队） | `GET /api/teams`（取 `data.list`）、`POST /api/teams`、`POST /api/teams/join`（6 位邀请码）、`POST /api/teams/leave`、`GET /api/teams/:id`、`GET /api/teams/ranking` |
| 字灵 AI | `POST /ziling/api/chat`、`POST /ziling/api/schedule`、`POST /ziling/api/validate`、`GET /ziling/api/ping`（在 iframe 内由 `js/ai/bridge.js` 发起，带 JWT，自动解包 `{code,data}`） |

**任务字段映射**（前端 ↔ 后端）：
`title↔title`、`date↔dueDate`(YYYY-MM-DD)、`tag↔category`（work→other / study→study / life→habit）、
完成状态：一次性任务（repeatType='none'，前端只建这种）看 `status==='completed'`。
本地任务记 `serverId` 对账：登录时 `GET /api/tasks` 拉取合并，本地未上云的自动补传。

---

## 三、哪些是前端本地实现（后端想接管时再加字段）

后端 Task 模型目前没有这些概念，前端把它们做成了**本地增强**（存 localStorage，不上传）：

| 功能 | 前端字段 | 建议后端将来加（可选） |
|---|---|---|
| 任务时间点 | `time`('HH:MM') | `Task.time` |
| 准点提醒/闹钟 | `remind/remindedAt/snoozeUntil`；15s 轮询 + 应用内闹钟卡片 + 提示音 + Notification | 提醒字段 + **Web Push**（进程被杀时也能推） |
| 任务指派 / 任务归队 | `assignee/teamId`（本地团队模式用） | `Task.teamId + assigneeId`（团队任务）|
| 搁置态 | `delayed` | `status:'paused'` 可直接映射 |

登录后团队页走服务器（邀请码/成员/积分/排行），任务保持"个人任务云同步"——与 zicodo
现有模型一致，没有强扭你们的表结构。

---

## 四、字灵 / 品牌（本仓库主体）要点

- **命名**：App 整体叫 **zicodo**；「字灵」只是其中的电子宠物（标题、manifest、
  开屏字标、通知标题等已统一）。
- `ziling.html` = 汉字精灵本体，App 用 iframe 嵌入（`?embed=1`）。
- 字灵开屏动画已**整体重做**（替换旧·电路注入版）：「青瓷墨滴成灵」——墨滴落下
  → 涟漪 → 沿圆周画出圆环 → 挑出双山眉眼（即品牌 logo）→ 眨眼两次 → 上浮淡出。
- AI 三态自动切换：后端网关（登录后走你们的 `/ziling/api/*`）→ 开发者自带 DeepSeek key →
  内置 Mock。后端不可用时静默回退 Mock，不会白屏。
- 你们的 `/ziling/api/chat|schedule|validate` 响应体（quickReply/megachar/stream、
  message/emoji、valid/word）与前端期望一致，**不需要改**。
- 品牌 logo：`icons/logo.svg`、眨眼版 `icons/logo-wink.svg`；已用于 PWA 图标/favicon/
  中央 tab/寄语卡头像/两种开屏/登录抽屉。
- **面向用户的界面没有任何服务器/密钥输入**：DeepSeek key 的 UI 已移除（联调需要时
  开发者可在控制台手动 `localStorage.setItem('ziling.deepseek.key','...')`）；
  后端地址同理只走 config.js / 同源 / `?api=`。

## 五、测试清单（建议照跑一遍）

1. `GET /api/health` 200。
2. Pages 链接打开 → 我的 → 登录/注册：出现表单（而非"未接入"）。
3. 注册 A、游客 B 两个账号；A 建队得邀请码，B 凭码加入；两端团队页都能看到彼此与积分排行。
4. A 新建任务 → 完成（打卡）→ 退出重进，任务状态在；换台设备登 A，任务同步下来。
5. 字灵页：和它说话 → 回复来自 DeepSeek（不是 Mock，可从语义判断/看后端日志）。
6. 队长退出 = 解散：前端已做二次确认，确认后整队消失（这是你们后端的语义）。

有问题先看浏览器 Console 的 `[sync]` / `[ai]` 前缀日志，全部失败均降级、不会阻塞 UI。
